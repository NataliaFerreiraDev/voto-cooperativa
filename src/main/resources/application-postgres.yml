spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/votocooperativa}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  h2:
    console:
      enabled: false